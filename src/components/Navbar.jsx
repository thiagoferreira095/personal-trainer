import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="logo">
                <img src="#!" alt="Logo do site" />
            </div>
        </nav>
    );
};

export default Navbar;